#########################################
# Зависимости и сгенерированные файлы
#########################################

# PHP-зависимости, установленные через Composer
/vendor/

# JavaScript-зависимости, установленные через npm/yarn
/node_modules/

# Если используется Vite (dev-сервер):
/public/hot

# Если ассеты публикуются в отдельную папку (например, symfony/mix или Vite):
/public/storage
# Если у вас публичная директория называется по-другому, например:
#/public_html  (раскомментируйте, если это действительно временные файлы)

#########################################
# Файлы окружения и конфигурации
#########################################
.env
.env.backup
.env.*.backup
.phpunit.result.cache
docker-compose.override.yml

#########################################
# Файлы, связанные с кэшированием и логами
 /storage/*.key

# Логи и временные файлы фреймворка
/storage/logs
/storage/framework/cache/data
/storage/framework/sessions
/storage/framework/views

# Файл кеша phpunit
.phpunit.result.cache

#########################################
# Прочие временные файлы
#########################################

# Логи от npm или yarn
npm-debug.log*
yarn-error.log*
